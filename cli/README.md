# ThousandEyes Recorder CLI

Provides the `te` command-line interface for recording and playing browser transactions.

```text
te record <url> <file>
te play <file>
```

Recording opens a browser and saves the recording document when Enter is pressed. Playback loads a recording document and plays it in a browser.
