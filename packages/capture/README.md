# Capture

Creates recordings from browser interactions.

Capture manages its Playwright browser session, injects recording controls and element highlighting, and produces recording artifacts. Hosts remain responsible for persistence and user experience.

## APIs

The package root exposes the recorder API.
